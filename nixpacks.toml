providers = ["python"]

# No Node.js needed — frontend/dist/ is pre-built and committed to git
[phases.build]
cmds = [
    "mkdir -p backend/static",
    "cp -r frontend/dist/* backend/static/"
]

[start]
cmd = "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
